{
    "name": "Magento Setup",
    "description": "Magento site setup",
    "authors": [
        {
            "name": "${deploy.author.name}"
            ,"email": "${deploy.author.email}"
        }
    ],
    "repositories": [
        {
            "type": "composer",
            "url": "http://packages.firegento.com"
        }
    ],
    "require": {
        "php": ">=5.3.3",
        "composer/installers": "~1.0",
        "magento/core": "${platform.magento.version}"
    },
    "extra": {
        "magento-root-dir":"public",
        "installer-paths": {

        }
    },
    "minimum-stability": "dev"
}